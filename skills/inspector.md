Skill dir: SKILL_DIR, port=PORT, session_id=SESSION_ID

Run INSPECTION_CODE in the kernel:
  bash SKILL_DIR/scripts/execute-code.sh --port PORT --session SESSION_ID -c "INSPECTION_CODE"

Return ONLY: {"result":"...concise summary..."}
